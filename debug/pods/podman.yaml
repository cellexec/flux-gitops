apiVersion: v1
kind: Pod
metadata:
  name: debug-podman
spec:
  securityContext:
    fsGroup: 1000
  containers:
    - name: debug-podman
      image: quay.io/podman/stable:latest
      command:
        - /bin/bash
      stdin: true
      tty: true
      volumeMounts:
        - mountPath: /tmp/container-storage
          name: container-storage
      securityContext:
        privileged: true
  volumes:
    - name: container-storage
      emptyDir: {}

