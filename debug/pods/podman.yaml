apiVersion: v1
kind: Pod
metadata:
  name: debug-podman
spec:
  containers:
    - name: debug-podman
      image: quay.io/podman/stable:latest
      command:
        - /bin/bash
      stdin: true
      tty: true
      volumeMounts:
        - mountPath: /tmp/container-storage
          name: container-storage
  volumes:
    - name: container-storage
      emptyDir: {}
